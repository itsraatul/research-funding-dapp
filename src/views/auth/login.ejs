<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Protocol Access // ResearChain</title>
    <link
      href="https://fonts.googleapis.com/css2?family=Roboto+Mono:wght@400;600;700&family=Inter:wght@400;600;800&display=swap"
      rel="stylesheet"
    />
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css"
    />
    <style>
      /* ------------------ Global Styles (Bright Corporate Tech Theme) ------------------ */
      :root {
        --color-primary: #0077ff; /* Electric Blue (Action) */
        --color-secondary: #00bcd4; /* Vibrant Cyan/Teal (Highlight) */
        --color-bg-light: #f7f9fc; /* Soft Light Blue-Gray Base */
        --color-bg-clean: #ffffff; /* Pure White Panel */
        --color-text-dark: #171a23; /* Dark Text */
        --color-text-muted: #6b778d; /* Muted Text */
        --font-mono: "Roboto Mono", monospace;
        --font-sans: "Inter", sans-serif;
      }
      * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
      }

      body {
        font-family: var(--font-sans);
        background-color: var(--color-bg-light);
        color: var(--color-text-dark);
        min-height: 100vh;
        display: flex;
        flex-direction: column;
        /* Subtle diagonal grid overlay on light background */
        background-image: url('data:image/svg+xml;utf8,<svg xmlns="http://www.w3.org/2000/svg" width="100%" height="100%" viewBox="0 0 100 100"><defs><pattern id="grid" width="15" height="15" patternUnits="userSpaceOnUse" patternTransform="rotate(45)"><path d="M 0 0 L 15 0 L 15 15 L 0 15 Z" fill="none" stroke="%23e5eaf1" stroke-width="0.5"/></pattern></defs><rect width="100%" height="100%" fill="url(%23grid)" /></svg>');
      }

      /* Header */
      header {
        background: var(--color-bg-clean);
        box-shadow: 0 4px 10px rgba(0, 0, 0, 0.05);
        padding: 18px 60px;
        display: flex;
        justify-content: space-between;
        align-items: center;
        position: sticky;
        top: 0;
        z-index: 10;
        border-bottom: 2px solid var(--color-secondary);
      }

      header h1 {
        font-family: var(--font-mono);
        font-size: 24px;
        font-weight: 700;
        color: var(--color-text-dark);
        letter-spacing: 1px;
      }
      header h1 span {
        color: var(--color-primary);
        font-size: 18px;
        margin-left: 5px;
      }

      nav a {
        color: var(--color-text-muted);
        text-decoration: none;
        font-weight: 600;
        margin-left: 20px;
        transition: color 0.25s ease;
      }

      nav a:hover {
        color: var(--color-primary);
      }

      /* Layout */
      main {
        flex: 1;
        display: flex;
        justify-content: center;
        align-items: center;
        padding: 50px 20px;
      }

      .login-panel {
        background: var(--color-bg-clean);
        border-radius: 12px;
        box-shadow: 0 15px 50px rgba(0, 0, 0, 0.1);
        display: flex;
        flex-wrap: wrap;
        overflow: hidden;
        max-width: 1000px;
        width: 100%;
        border: 1px solid #e0e0e0;
      }

      /* Left visual section (The Authority Panel) */
      .login-visual {
        flex: 1 1 45%;
        /* Bright, energetic gradient */
        background: linear-gradient(145deg, var(--color-secondary), #00a4b3);
        color: #fff;
        display: flex;
        flex-direction: column;
        justify-content: center;
        padding: 50px;
        position: relative;
        border-right: 1px solid #c9c9c9;
      }

      .login-visual i {
        font-size: 40px;
        margin-bottom: 20px;
        color: var(--color-primary);
        text-shadow: 0 0 10px rgba(0, 119, 255, 0.8);
      }

      .login-visual h2 {
        font-family: var(--font-sans);
        font-size: 28px;
        font-weight: 700;
        margin-bottom: 10px;
        letter-spacing: 0.5px;
      }

      .login-visual p {
        font-size: 16px;
        line-height: 1.6;
        opacity: 0.95;
      }

      .login-visual p.status-badge {
        font-family: var(--font-mono);
        font-size: 13px;
        margin-top: 25px;
        color: var(--color-primary);
        background: rgba(255, 255, 255, 0.9);
        border: 1px solid var(--color-primary);
        padding: 6px 12px;
        width: fit-content;
        border-radius: 4px;
        font-weight: 700;
      }

      /* Right form section */
      .login-form {
        flex: 1 1 55%;
        padding: 50px 60px;
        background-color: var(--color-bg-clean);
      }

      h3 {
        font-size: 30px;
        margin-bottom: 35px;
        color: var(--color-text-dark);
        font-weight: 800;
      }

      form {
        display: flex;
        flex-direction: column;
        gap: 25px;
      }

      .input-group {
        display: flex;
        flex-direction: column;
        gap: 8px;
      }

      label {
        font-family: var(--font-sans);
        font-size: 13px;
        font-weight: 600;
        color: var(--color-text-muted);
        text-transform: uppercase;
        letter-spacing: 0.5px;
      }

      input {
        width: 100%;
        padding: 14px 16px;
        border: 1px solid #d1d9e6;
        border-radius: 8px; /* Slightly rounded for softness */
        font-size: 16px;
        background: var(--color-bg-light);
        color: var(--color-text-dark);
        transition: all 0.25s ease;
      }

      input:focus {
        border-color: var(--color-primary);
        outline: none;
        background: var(--color-bg-clean);
        box-shadow: 0 0 0 3px rgba(0, 119, 255, 0.2);
      }

      button {
        margin-top: 20px;
        background: var(--color-primary);
        color: var(--color-bg-clean);
        padding: 15px;
        border: none;
        border-radius: 8px;
        font-size: 17px;
        font-weight: 700;
        cursor: pointer;
        transition: all 0.3s ease;
        letter-spacing: 0.5px;
        text-transform: uppercase;
        box-shadow: 0 4px 20px rgba(0, 119, 255, 0.3);
      }

      button:hover {
        background: #0059b3;
        transform: translateY(-2px);
        box-shadow: 0 8px 30px rgba(0, 119, 255, 0.5);
      }

      .link {
        text-align: center;
        margin-top: 30px;
        font-size: 14px;
        color: var(--color-text-muted);
      }

      .link a {
        color: var(--color-accent);
        text-decoration: none;
        font-weight: 600;
        transition: color 0.2s;
      }

      .link a:hover {
        color: var(--color-secondary);
        text-decoration: underline;
      }

      footer {
        background: var(--color-bg-clean);
        text-align: center;
        padding: 20px;
        font-size: 13px;
        color: var(--color-text-muted);
        border-top: 1px solid #e2e8f0;
        font-family: var(--font-mono);
      }

      @media (max-width: 900px) {
        .login-panel {
          flex-direction: column;
          max-width: 450px;
        }
        .login-visual {
          flex: 1 1 100%;
          padding: 40px;
          text-align: center;
          border-right: none;
          border-bottom: 1px solid #c9c9c9;
        }
        .login-visual i {
          font-size: 35px;
          margin-bottom: 15px;
        }
        .login-visual p.status-badge {
          margin: 15px auto 0;
        }
        .login-form {
          padding: 40px 30px;
        }
      }
    </style>
  </head>

  <body>
    <header>
      <h1>ResearChain<span>.io</span></h1>
      <nav>
        <a href="/">HOME</a>
        <a href="/register">REGISTER</a>
        <a href="/public/dashboard">DASHBOARD</a>
      </nav>
    </header>

    <main>
      <div class="login-panel">
        <div class="login-visual">
          <i class="fas fa-lock-open"></i>
          <h2>PROTOCOL ACCESS REQUIRED</h2>
          <p>
            Authenticate to access the core ResearChain ledger. This portal is
            secured by immutable blockchain proofs to protect all registered
            Intellectual Property (IP) and escrowed funding contracts.
          </p>
          <p class="status-badge">
            <i class="fas fa-cubes"></i> LEDGER STATUS: SYNCHRONIZED
          </p>
        </div>

        <div class="login-form">
          <h3>AUTHENTICATION</h3>

          <form method="POST" action="/login">
            <div class="input-group">
              <label for="email">User Email (ID)</label>
              <input
                type="email"
                id="email"
                name="email"
                required
                placeholder="name@corporation.com"
              />
            </div>

            <div class="input-group">
              <label for="password">Password</label>
              <input
                type="password"
                id="password"
                name="password"
                required
                placeholder="••••••••••••"
              />
            </div>

            <button type="submit">
              <i class="fas fa-arrow-circle-right"></i> EXECUTE LOGIN
            </button>
          </form>

          <div class="link">
            Unauthorized?
            <a href="/register">Request Network Access</a>
          </div>
        </div>
      </div>
    </main>

    <footer>PROTOCOL VERSION 2.0 // IMMUTABLE LEDGER // &copy; 2025</footer>
  </body>
</html>
