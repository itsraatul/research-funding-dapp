<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Browse Verified Projects | ResearChain Capital</title>
    <!-- Google Font for high-tech, serious look -->
    <link
      href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700;800&family=Space+Mono:wght@400;700&display=swap"
      rel="stylesheet"
    />
    <!-- Font Awesome for sleek icons -->
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css"
    />
    <style>
      /* ------------------ Global Styles (ResearChain Funder Theme) ------------------ */
      :root {
        --color-accent: #0077ff; /* Electric Blue (Action/Primary) */
        --color-secondary: #00bcd4; /* Vibrant Cyan/Teal (Highlight) */
        --color-success: #27ae60; /* Verified Green */
        --color-funded: #0059b3; /* Funded Blue (Darker shade) */
        --color-text-primary: #171a23;
        --color-text-secondary: #6b778d;
        --color-bg-light: #f7f9fc;
        --color-bg-clean: #ffffff;
        --font-mono: "Space Mono", monospace;
        --font-sans: "Inter", sans-serif;
      }
      /* ---------------- GLOBAL ---------------- */
      * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
      }

      body {
        font-family: var(--font-sans);
        background-color: var(--color-bg-light);
        color: var(--color-text-primary);
        min-height: 100vh;
      }

      a {
        text-decoration: none;
        color: var(--color-accent);
      }

      header {
        background: var(--color-bg-clean);
        box-shadow: 0 1px 10px rgba(0, 0, 0, 0.05);
        padding: 18px 60px;
        display: flex;
        justify-content: space-between;
        align-items: center;
        position: sticky;
        top: 0;
        z-index: 10;
        border-bottom: 2px solid var(--color-secondary);
      }

      header h1 {
        font-family: var(--font-mono);
        font-size: 24px;
        font-weight: 700;
        color: var(--color-text-primary);
        letter-spacing: 1px;
      }
      header h1 span {
        color: var(--color-secondary);
      }

      nav a {
        color: var(--color-text-secondary);
        font-weight: 600;
        margin-left: 25px;
        transition: color 0.3s ease;
      }

      nav a:hover {
        color: var(--color-accent);
      }

      /* ---------------- CONTAINER ---------------- */
      .container {
        max-width: 1250px;
        margin: 40px auto;
        padding: 0 20px 80px;
      }

      h2.page-title {
        text-align: center;
        font-size: 32px;
        font-weight: 800;
        color: var(--color-text-primary);
        margin-bottom: 40px;
        letter-spacing: -0.5px;
      }

      /* ---------------- GRID ---------------- */
      .grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
        gap: 30px;
      }

      /* ---------------- PROJECT CARD ---------------- */
      .project-card {
        background: var(--color-bg-clean);
        border-radius: 12px;
        box-shadow: 0 8px 20px rgba(0, 0, 0, 0.08);
        padding: 30px;
        display: flex;
        flex-direction: column;
        justify-content: space-between;
        transition: all 0.3s ease;
        border-top: 4px solid var(--color-secondary);
      }

      .project-card:hover {
        transform: translateY(-5px);
        box-shadow: 0 15px 35px rgba(0, 0, 0, 0.15);
      }

      .project-header {
        display: flex;
        justify-content: space-between;
        align-items: flex-start;
        margin-bottom: 15px;
      }

      .project-header h3 {
        font-size: 22px;
        color: var(--color-text-primary);
        font-weight: 700;
        margin: 0;
        max-width: 80%;
      }

      .status {
        padding: 5px 10px;
        border-radius: 4px;
        font-size: 11px;
        font-weight: 700;
        color: #fff;
        text-transform: uppercase;
        align-self: flex-start;
      }

      .status.pending {
        background: #f59e0b;
      }
      .status.verified {
        background: var(--color-success);
      }
      .status.funded {
        background: var(--color-funded);
      }
      .status.rejected {
        background: var(--color-danger);
      }

      .project-details {
        flex-grow: 1;
      }

      .project-card p {
        font-size: 15px;
        color: var(--color-text-secondary);
        line-height: 1.6;
        margin-bottom: 12px;
      }

      .meta-data {
        font-size: 14px;
        color: var(--color-text-muted);
        margin-top: 10px;
        border-top: 1px solid #f0f0f0;
        padding-top: 10px;
      }

      .ipfs-link {
        color: var(--color-secondary);
        font-weight: 600;
        font-family: var(--font-mono);
        font-size: 13px;
        margin-left: 5px;
      }

      .project-footer {
        margin-top: 20px;
      }

      /* ---------------- BUTTON ---------------- */
      .btn {
        padding: 12px 20px;
        border: none;
        border-radius: 8px;
        cursor: pointer;
        font-size: 15px;
        font-weight: 700;
        transition: all 0.3s ease;
        text-transform: uppercase;
        box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
      }

      .btn-primary {
        background: var(--color-accent);
        color: #fff;
      }

      .btn-primary:hover {
        transform: translateY(-2px);
        background: #0059b3;
        box-shadow: 0 8px 20px rgba(0, 119, 255, 0.35);
      }

      .btn-disabled {
        background: #e2e8f0;
        color: #94a3b8;
        cursor: not-allowed;
        box-shadow: none;
      }

      /* Empty state */
      .empty-state {
        text-align: center;
        color: var(--color-text-secondary);
        margin-top: 50px;
        font-size: 18px;
      }

      footer {
        background: var(--color-bg-clean);
        border-top: 1px solid #e2e8f0;
        text-align: center;
        padding: 25px;
        color: var(--color-text-secondary);
        font-size: 13px;
        font-family: var(--font-mono);
      }

      @media (max-width: 768px) {
        header {
          padding: 18px 25px;
        }
        h2.page-title {
          font-size: 28px;
        }
        .grid {
          grid-template-columns: 1fr;
        }
      }
    </style>
  </head>

  <body>
    <header>
      <h1>ResearChain<span>.io</span></h1>
      <nav>
        <a href="/">HOME</a>
        <a href="/company/dashboard">DASHBOARD</a>
        <a href="/logout">LOGOUT</a>
      </nav>
    </header>

    <div class="container">
      <h2 class="page-title">
        <i class="fas fa-project-diagram"></i> Discover Verified Research for
        Funding
      </h2>

      <% if (projects.length === 0) { %>
      <p class="empty-state">
        <i class="fas fa-inbox" style="margin-right: 10px"></i>
        No verified projects are currently available for funding. Check back
        soon!
      </p>
      <% } else { %>
      <div class="grid">
        <% projects.forEach(project => { %>
        <div class="project-card">
          <div class="project-header">
            <h3><%= project.title %></h3>
            <span class="status <%= project.status %>"
              ><%= project.status.toUpperCase() %></span
            >
          </div>

          <div class="project-details">
            <p>**Abstract:** <%= project.abstract.substring(0, 180) %>...</p>
            <p>**Researcher:** <%= project.owner?.name || "Unknown" %></p>
            <p>
              **Verification Authority:**
              <strong><%= project.university?.name || "N/A" %></strong>
            </p>
          </div>

          <div class="meta-data">
            <i class="fas fa-calendar-alt"></i> Uploaded: <%= new
            Date(project.createdAt).toLocaleDateString() %>
          </div>

          <div class="project-footer">
            <div style="margin-bottom: 10px">
              <i class="fas fa-file-invoice"></i>
              <a
                href="https://ipfs.io/ipfs/<%= project.ipfsHash %>"
                target="_blank"
                class="ipfs-link"
              >
                View Full Proposal (IPFS Hash)
              </a>
            </div>

            <% if (project.status === "verified") { %>
            <form action="/company/fund/<%= project._id %>" method="GET">
              <button class="btn btn-primary">
                <i class="fas fa-hand-holding-usd"></i> PROPOSE FUNDING
              </button>
            </form>
            <% } else { %>
            <button class="btn btn-disabled">
              <i class="fas fa-check-circle"></i> PROJECT FUNDED
            </button>
            <% } %>
          </div>
        </div>
        <% }) %>
      </div>
      <% } %>
    </div>

    <footer>PROTOCOL VERSION 2.0 // CAPITAL LEDGER // &copy; 2025</footer>
  </body>
</html>
