<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Funder Console | ResearChain Capital Management</title>
    <!-- Google Font for high-tech, serious look -->
    <link
      href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700;800&family=Space+Mono:wght@400;700&display=swap"
      rel="stylesheet"
    />
    <!-- Font Awesome for sleek icons -->
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css"
    />
    <style>
      /* ------------------ Global Styles (ResearChain Funder Theme) ------------------ */
      :root {
        --color-accent: #0077ff; /* Electric Blue (Action/Primary) */
        --color-secondary: #00bcd4; /* Vibrant Cyan/Teal (Highlight) */
        --color-success: #27ae60; /* Completed/Released Green */
        --color-danger: #e74c3c; /* Refund Red */
        --color-text-primary: #171a23;
        --color-text-secondary: #6b778d;
        --color-bg-light: #f7f9fc;
        --color-bg-clean: #ffffff;
        --font-mono: "Space Mono", monospace;
        --font-sans: "Inter", sans-serif;
      }
      * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
      }

      body {
        font-family: var(--font-sans);
        background-color: var(--color-bg-light);
        color: var(--color-text-primary);
        min-height: 100vh;
      }

      a {
        text-decoration: none;
        color: var(--color-accent);
      }

      /* ============ HEADER ============ */
      header {
        background: var(--color-bg-clean);
        box-shadow: 0 1px 10px rgba(0, 0, 0, 0.05);
        padding: 18px 60px;
        display: flex;
        justify-content: space-between;
        align-items: center;
        position: sticky;
        top: 0;
        z-index: 10;
        border-bottom: 2px solid var(--color-secondary);
      }

      header h1 {
        font-family: var(--font-mono);
        font-size: 24px;
        font-weight: 700;
        color: var(--color-text-primary);
        letter-spacing: 1px;
      }
      header h1 span {
        color: var(--color-secondary);
      }

      nav a {
        color: var(--color-text-secondary);
        font-weight: 600;
        margin-left: 25px;
        transition: color 0.3s;
      }

      nav a:hover {
        color: var(--color-accent);
      }

      /* ============ CONTAINER ============ */
      .container {
        max-width: 1200px;
        margin: 40px auto;
        padding: 0 20px 60px;
      }

      .dashboard-header {
        display: flex;
        justify-content: space-between;
        align-items: center;
        flex-wrap: wrap;
        gap: 15px;
        margin-bottom: 35px;
        border-bottom: 1px solid #e0e0e0;
        padding-bottom: 20px;
      }

      .dashboard-header h2 {
        font-size: 32px;
        font-weight: 800;
        color: var(--color-text-primary);
        letter-spacing: -0.5px;
      }

      .action-buttons {
        display: flex;
        flex-wrap: wrap;
        gap: 12px;
      }

      .btn {
        padding: 12px 22px;
        border-radius: 8px;
        font-weight: 600;
        font-size: 15px;
        cursor: pointer;
        transition: all 0.3s ease;
        border: none;
        text-transform: uppercase;
        box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
      }

      .btn-primary {
        background: var(--color-accent);
        color: #fff;
        box-shadow: 0 4px 15px rgba(0, 119, 255, 0.25);
      }

      .btn-primary:hover {
        transform: translateY(-2px);
        background: #0059b3;
        box-shadow: 0 8px 25px rgba(0, 119, 255, 0.35);
      }

      .btn-secondary {
        background: var(--color-secondary);
        color: var(--color-text-primary);
      }

      .btn-secondary:hover {
        background: #00a4b3;
        color: var(--color-bg-clean);
      }

      /* ============ SUMMARY CARDS ============ */
      .summary {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(260px, 1fr));
        gap: 20px;
        margin-bottom: 40px;
      }

      .card {
        background: var(--color-bg-clean);
        border-radius: 12px;
        padding: 25px;
        box-shadow: 0 8px 20px rgba(0, 0, 0, 0.08);
        transition: all 0.3s ease;
        text-align: center;
        border-bottom: 4px solid var(--color-accent);
      }
      .card:nth-child(2) {
        border-bottom-color: var(--color-secondary);
      }
      .card:nth-child(3) {
        border-bottom-color: var(--color-success);
      }

      .card:hover {
        transform: translateY(-4px);
        box-shadow: 0 12px 30px rgba(0, 0, 0, 0.12);
      }

      .card h3 {
        font-size: 36px;
        color: var(--color-primary);
        margin-bottom: 6px;
        font-weight: 800;
      }

      .card p {
        font-size: 15px;
        color: var(--color-text-secondary);
        font-weight: 500;
        text-transform: uppercase;
      }

      /* ============ PROJECTS TABLE CONTAINER ============ */
      .project-table-container {
        background: var(--color-bg-clean);
        border-radius: 12px;
        overflow: hidden;
        box-shadow: 0 8px 25px rgba(0, 0, 0, 0.08);
      }

      /* ============ TABLE ============ */
      table {
        width: 100%;
        border-collapse: collapse;
      }

      th,
      td {
        padding: 16px;
        text-align: left;
        vertical-align: middle;
      }

      th {
        background: var(--color-text-primary);
        color: var(--color-bg-light);
        text-transform: uppercase;
        font-size: 13px;
        letter-spacing: 0.8px;
        font-weight: 700;
      }

      tr:nth-child(even) {
        background: var(--color-bg-light);
      }

      tr:hover {
        background: #e9eff5;
      }

      td {
        font-size: 14px;
        color: var(--color-text-primary);
      }

      /* ============ STATUS TAGS ============ */
      .status-tag {
        padding: 6px 10px;
        border-radius: 6px;
        font-size: 12px;
        font-weight: 700;
        color: #fff;
        text-transform: uppercase;
      }

      .status-tag.pending {
        background: #f59e0b;
      }
      .status-tag.verified {
        background: var(--color-secondary);
      }
      .status-tag.rejected {
        background: var(--color-danger);
      }
      .status-tag.funded {
        background: var(--color-accent);
      }
      .status-tag.released,
      .status-tag.completed {
        background: var(--color-success);
      }
      .status-tag.pending-release {
        background: #3b82f6;
      }

      /* ============ ACTION BUTTONS ============ */
      .btn-table {
        display: inline-block;
        padding: 8px 14px;
        border-radius: 6px;
        font-size: 13px;
        font-weight: 600;
        margin-right: 6px;
        transition: 0.2s;
      }

      .btn-view {
        background: var(--color-text-primary);
        color: #fff;
      }

      .btn-view:hover {
        background: var(--color-secondary);
        color: var(--color-text-primary);
      }

      .btn-refund {
        background: var(--color-danger);
        color: #fff;
        border: none;
      }

      .btn-refund:hover {
        background: #cc3728;
      }

      .no-action {
        color: var(--color-text-secondary);
        font-size: 13px;
      }

      /* ============ MILESTONES TABLE ============ */
      .milestone-table {
        width: 100%;
        margin: 0;
        border-collapse: collapse;
        font-size: 13px;
        background: #f0f3f6;
        border-top: 3px solid var(--color-secondary);
      }

      .milestone-table th,
      .milestone-table td {
        border: 1px solid #e2e8f0;
        padding: 10px;
      }

      .milestone-table th {
        background: #e0eaf3;
        color: var(--color-text-primary);
        font-weight: 700;
        text-transform: capitalize;
        letter-spacing: 0;
      }
      .milestone-table td {
        color: #4a5c76;
      }

      .proof-link {
        color: var(--color-accent);
        font-weight: 500;
        font-family: var(--font-mono);
      }

      /* FOOTER */
      footer {
        background: var(--color-bg-clean);
        border-top: 1px solid #e2e8f0;
        text-align: center;
        padding: 25px;
        color: var(--color-text-secondary);
        font-size: 13px;
        font-family: var(--font-mono);
      }

      @media (max-width: 768px) {
        header {
          padding: 18px 25px;
        }
        .dashboard-header {
          flex-direction: column;
          text-align: center;
        }
        .action-buttons {
          justify-content: center;
        }
        .btn {
          width: 48%;
        }
        .summary {
          grid-template-columns: 1fr;
        }
        .project-table-container {
          overflow-x: auto;
        }
        table {
          min-width: 700px;
        }
        .milestone-table {
          min-width: 100%;
        }
      }
    </style>
  </head>

  <body>
    <header>
      <h1>ResearChain<span>.io</span></h1>
      <nav>
        <a href="/">HOME</a>
        <a href="/logout">LOGOUT</a>
      </nav>
    </header>

    <div class="container">
      <div class="dashboard-header">
        <h2><i class="fas fa-building"></i> Funder Capital Console</h2>
        <div class="action-buttons">
          <a href="/company/fund" class="btn btn-primary"
            ><i class="fas fa-hand-holding-usd"></i> FUND NEW PROJECT</a
          >
          <a href="/company/browse" class="btn btn-secondary"
            ><i class="fas fa-search"></i> BROWSE RESEARCH</a
          >
        </div>
      </div>

      <!-- Summary Section -->
      <div class="summary">
        <div class="card">
          <p>Projects Funded</p>
          <h3 style="color: var(--color-accent)"><%= stats.totalProjects %></h3>
        </div>
        <div class="card">
          <p>Total Capital Escrowed (ETH)</p>
          <h3 style="color: var(--color-secondary)">
            <%= stats.totalEthFunded %> ETH
          </h3>
        </div>
        <div class="card">
          <p>Milestones Verified & Released</p>
          <h3 style="color: var(--color-success)">
            <%= stats.releasedMilestones %>
          </h3>
        </div>
      </div>

      <!-- Projects Table Container -->
      <div class="project-table-container">
        <table>
          <thead>
            <tr>
              <th>Project Title</th>
              <th>Researcher</th>
              <th>Smart Contract (Escrow)</th>
              <th>Status</th>
              <th>Actions</th>
            </tr>
          </thead>
          <tbody>
            <% projects.forEach(p => { %>
            <tr>
              <td><%= p.title %></td>
              <td><%= p.owner?.name || "Unknown" %></td>
              <td style="font-family: var(--font-mono); font-size: 13px">
                <%= p.escrowAddress || "—" %>
              </td>
              <td>
                <span class="status-tag <%= p.status %>"
                  ><%= p.status.toUpperCase() %></span
                >
              </td>
              <td>
                <a href="#project-<%= p._id %>" class="btn-table btn-view">
                  <i class="fas fa-eye"></i> Details
                </a>
                <% if (p.escrowAddress && p.status === "funded") { %>
                <form
                  method="POST"
                  action="/company/refund/<%= p._id %>"
                  style="display: inline"
                >
                  <button type="submit" class="btn-table btn-refund">
                    <i class="fas fa-undo-alt"></i> REFUND
                  </button>
                </form>
                <% } else { %>
                <span class="no-action">No immediate action</span>
                <% } %>
              </td>
            </tr>

            <!-- Detailed Milestone Row (Initially hidden, revealed by anchor link) -->
            <tr id="project-<%= p._id %>">
              <td colspan="5" style="padding: 0">
                <% if (p.milestones && p.milestones.length > 0) { %>
                <table class="milestone-table">
                  <thead>
                    <tr>
                      <th>#</th>
                      <th>Milestone Title</th>
                      <th>% Complete</th>
                      <th>Amount (ETH)</th>
                      <th>Verification Status</th>
                      <th>Proof Link</th>
                    </tr>
                  </thead>
                  <tbody>
                    <% p.milestones.forEach((m, i) => { %>
                    <tr>
                      <td><%= i + 1 %></td>
                      <td><%= m.title || "Untitled" %></td>
                      <td><%= m.percentage || 0 %>%</td>
                      <td style="font-weight: 600"><%= m.amount || "—" %></td>
                      <td>
                        <span
                          class="status-tag <%= m.status === 'approved' && m.released ? 'released' : m.status %>"
                        >
                          <%= m.status === 'approved' && m.released ? 'RELEASED'
                          : (m.status ? m.status.toUpperCase() : 'PENDING') %>
                        </span>
                      </td>
                      <td>
                        <% if (m.proof) { %>
                        <a
                          href="https://ipfs.io/ipfs/<%= m.proof %>"
                          target="_blank"
                          class="proof-link"
                        >
                          <i class="fas fa-file-export"></i> View Proof
                        </a>
                        <% } else { %> — <% } %>
                      </td>
                    </tr>
                    <% }) %>
                  </tbody>
                </table>
                <% } else { %>
                <em
                  style="
                    color: var(--color-text-secondary);
                    display: block;
                    padding: 15px;
                    text-align: center;
                  "
                >
                  No milestones defined for this contract.
                </em>
                <% } %>
              </td>
            </tr>
            <% }) %>
          </tbody>
        </table>
      </div>
    </div>

    <footer>PROTOCOL VERSION 2.0 // CAPITAL LEDGER // &copy; 2025</footer>
  </body>
</html>
