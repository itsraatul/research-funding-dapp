<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>University Dashboard | ResearChain Verifier</title>
    <!-- Google Font for clean, professional look -->
    <link
      href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700&family=Space+Mono:wght@400;700&display=swap"
      rel="stylesheet"
    />
    <!-- Font Awesome for sleek icons -->
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css"
    />
    <style>
      /* ------------------ Global Styles (ResearChain Theme) ------------------ */
      :root {
        --color-primary: #0077ff; /* Electric Blue (Action) */
        --color-secondary: #00bcd4; /* Vibrant Cyan/Teal (Highlight) */
        --color-success: #27ae60; /* Approve/Verified Green */
        --color-danger: #e74c3c; /* Reject Red */
        --color-pending: #f39c12; /* Pending Orange */
        --color-text-primary: #171a23;
        --color-text-secondary: #6b778d;
        --color-bg-light: #f7f9fc;
        --color-bg-clean: #ffffff;
        --font-mono: "Space Mono", monospace;
        --font-sans: "Inter", sans-serif;
      }
      * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
      }
      body {
        font-family: var(--font-sans);
        margin: 0;
        background-color: var(--color-bg-light);
        color: var(--color-text-primary);
        min-height: 100vh;
      }
      a {
        text-decoration: none;
        color: var(--color-primary);
      }

      /* ------------------ Header ------------------ */
      header {
        background: var(--color-bg-clean);
        box-shadow: 0 2px 8px rgba(0, 0, 0, 0.08);
        padding: 18px 40px;
        display: flex;
        justify-content: space-between;
        align-items: center;
        border-bottom: 3px solid var(--color-secondary);
      }
      header h1 {
        font-family: var(--font-mono);
        font-size: 24px;
        font-weight: 700;
        color: var(--color-text-primary);
      }
      header h1 span {
        color: var(--color-accent);
      }
      nav a {
        color: var(--color-text-secondary);
        font-weight: 600;
        margin-left: 20px;
      }
      nav a:hover {
        color: var(--color-primary);
      }

      /* ------------------ Layout & Container ------------------ */
      .page-title {
        text-align: center;
        margin: 40px auto 20px;
        font-size: 32px;
        font-weight: 800;
        color: var(--color-text-primary);
        letter-spacing: -0.5px;
      }
      .container {
        max-width: 1000px;
        margin: 0 auto 60px;
        padding: 20px;
      }

      /* ------------------ Project Card Styling ------------------ */
      .project-card {
        background: var(--color-bg-clean);
        padding: 30px;
        margin-bottom: 30px;
        border-radius: 12px;
        box-shadow: 0 6px 20px rgba(0, 0, 0, 0.08);
        border-left: 5px solid var(--color-primary);
        transition: box-shadow 0.3s;
      }
      .project-card:hover {
        box-shadow: 0 10px 30px rgba(0, 0, 0, 0.12);
      }
      .project-card h2 {
        color: var(--color-primary);
        margin-bottom: 15px;
        font-size: 26px;
      }
      .project-details p {
        margin-bottom: 8px;
        font-size: 15px;
        color: var(--color-text-secondary);
      }
      .project-details strong {
        color: var(--color-text-primary);
      }

      /* ------------------ Action & Status ------------------ */
      .action-buttons {
        margin-top: 20px;
      }
      .btn {
        padding: 10px 18px;
        margin-right: 10px;
        border: none;
        border-radius: 6px;
        cursor: pointer;
        font-size: 15px;
        font-weight: 600;
        transition: transform 0.2s, box-shadow 0.2s;
        text-transform: uppercase;
        letter-spacing: 0.5px;
      }
      .approve {
        background: var(--color-success);
        color: white;
      }
      .reject {
        background: var(--color-danger);
        color: white;
      }
      .btn:hover {
        transform: translateY(-2px);
        box-shadow: 0 4px 10px rgba(0, 0, 0, 0.2);
      }

      .status-badge {
        font-weight: 700;
        padding: 4px 10px;
        border-radius: 4px;
        display: inline-block;
        font-size: 13px;
        letter-spacing: 0.5px;
      }
      .status.pending {
        background: #fff8e1;
        color: var(--color-pending);
      }
      .status.submitted {
        background: #e1f5fe;
        color: var(--color-secondary);
      }
      .status.approved,
      .status.released {
        background: #e8f5e9;
        color: var(--color-success);
      }
      .status.rejected {
        background: #ffebee;
        color: var(--color-danger);
      }

      /* ------------------ Milestone Section ------------------ */
      .milestone-section {
        margin-top: 30px;
        padding-top: 20px;
        border-top: 1px dashed #e0e0e0;
      }
      .milestone-section h3 {
        font-size: 20px;
        margin-bottom: 15px;
        color: var(--color-text-primary);
      }
      .milestone {
        margin-bottom: 15px;
        padding: 15px;
        background: var(--color-bg-light);
        border-radius: 8px;
        border-left: 4px solid var(--color-secondary);
      }
      .milestone p {
        margin-bottom: 5px;
      }

      .proof-link {
        font-family: var(--font-mono);
        font-size: 13px;
        color: var(--color-primary);
        display: inline-block;
        margin-right: 15px;
      }
      .proof-link i {
        margin-right: 5px;
      }

      /* ------------------ No Projects ------------------ */
      .no-projects {
        text-align: center;
        padding: 50px;
        background: var(--color-bg-clean);
        border-radius: 12px;
        box-shadow: 0 6px 20px rgba(0, 0, 0, 0.05);
      }
    </style>
  </head>
  <body>
    <header>
      <h1>ResearChain<span>.io</span></h1>
      <nav>
        <a href="/">Home</a>
        <a href="/logout">Logout</a>
        <a href="/university/verify-students" class="btn"
          >🔍 Verify Student Identities</a
        >
      </nav>
    </header>

    <h2 class="page-title">
      <i class="fas fa-university"></i> University Verification Dashboard
    </h2>

    <div class="container">
      <% if (projects.length === 0) { %>
      <div class="no-projects">
        <p>
          <i
            class="fas fa-check-circle"
            style="color: var(--color-success); margin-right: 10px"
          ></i>
          No projects require your review at the moment. All systems are green.
        </p>
      </div>
      <% } else { %> <% projects.forEach(project => { %>
      <div class="project-card">
        <h2><%= project.title %></h2>
        <div class="project-details">
          <p>
            <strong>Researcher:</strong> <%= project.owner?.name || "Unknown" %>
          </p>
          <p>
            <strong>Status:</strong>
            <span class="status-badge status <%= project.status %>"
              ><%= project.status.toUpperCase() %></span
            >
          </p>
          <p>
            <strong>Abstract:</strong>
            <span style="color: var(--color-text-secondary)"
              ><%= project.abstract %></span
            >
          </p>
        </div>

        <% if (project.status === "pending") { %>
        <!-- Project verification buttons -->
        <div class="action-buttons">
          <p style="margin-bottom: 10px; font-weight: 600">
            Action Required: Verify Project Authenticity
          </p>
          <form
            action="/university/approve/<%= project._id %>"
            method="POST"
            style="display: inline"
          >
            <button class="btn approve" type="submit">
              <i class="fas fa-check"></i> VERIFY & APPROVE
            </button>
          </form>
          <form
            action="/university/reject/<%= project._id %>"
            method="POST"
            style="display: inline"
          >
            <button class="btn reject" type="submit">
              <i class="fas fa-times"></i> REJECT PROJECT
            </button>
          </form>
        </div>
        <% } %>

        <!-- Milestone Review Section -->
        <div class="milestone-section">
          <% if (project.milestones && project.milestones.length > 0) { %>
          <h3><i class="fas fa-tasks"></i> Milestone Review Log:</h3>

          <% project.milestones.forEach((m, index) => { const proofCid = m.proof
          || m.ipfsHash; const canModerate = proofCid && !["approved",
          "rejected", "released"].includes(m.status); %>
          <div class="milestone">
            <p>
              <strong>Milestone #<%= index + 1 %>:</strong> <%= m.title ||
              "Untitled" %>
            </p>
            <p><strong>Payout Share:</strong> <%= m.percentage || 0 %>%</p>
            <p>
              <strong>Current Status:</strong>
              <span class="status-badge status <%= m.status %>">
                <%= m.status ? m.status.toUpperCase() : "PENDING" %>
              </span>
            </p>

            <% if (proofCid) { %>
            <a
              href="https://ipfs.io/ipfs/<%= proofCid %>"
              target="_blank"
              class="proof-link"
            >
              <i class="fas fa-external-link-alt"></i> REVIEW IPFS PROOF
            </a>
            <% } else { %>
            <p style="color: var(--color-danger); font-weight: 500">
              No proof uploaded by researcher yet.
            </p>
            <% } %>

            <div class="action-buttons" style="margin-top: 10px">
              <% if (canModerate) { %>
              <form
                action="/university/<%= project._id %>/milestone/<%= index %>/approve"
                method="POST"
                style="display: inline"
              >
                <button class="btn approve" type="submit">
                  <i class="fas fa-thumbs-up"></i> APPROVE MILESTONE
                </button>
              </form>
              <form
                action="/university/<%= project._id %>/milestone/<%= index %>/reject"
                method="POST"
                style="display: inline"
              >
                <button class="btn reject" type="submit">
                  <i class="fas fa-hand-point-down"></i> REJECT PROOF
                </button>
              </form>
              <% } else if (m.status === "approved" || m.status === "released")
              { %>
              <p style="color: var(--color-success); font-weight: 600">
                <i class="fas fa-check-double"></i> Verified. Funds either
                pending release or already disbursed.
              </p>
              <% } else if (m.status === "rejected") { %>
              <p style="color: var(--color-danger); font-weight: 600">
                <i class="fas fa-times-circle"></i> Proof rejected. Awaiting
                student re-submission.
              </p>
              <% } %>
            </div>
          </div>
          <% }) %> <% } else { %>
          <p style="color: var(--color-text-secondary); margin-left: 20px">
            No milestones have been defined for this project yet.
          </p>
          <% } %>
        </div>
      </div>
      <% }) %> <% } %>
    </div>
  </body>
</html>
